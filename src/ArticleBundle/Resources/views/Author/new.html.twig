{% extends 'base.html.twig' %}

{% block body %}
    <h1>New author</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('author_index') }}">Back to the authors list</a>
        </li>
    </ul>
{% endblock %}
