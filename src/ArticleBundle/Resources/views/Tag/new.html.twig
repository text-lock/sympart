{% extends 'base.html.twig' %}

{% block body %}
    <h1>New tag</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('tag_index') }}">Back to the tag list</a>
        </li>
    </ul>
{% endblock %}
